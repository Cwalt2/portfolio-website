# Hugo Portfolio


This is a starter scaffold for a Hugo-based portfolio. Files in this canvas show the critical pieces to get a professional site up quickly.


## Steps
1. Copy files into your Hugo site folder exactly as paths indicate.
2. Add images to `static/img/`.
3. Run `hugo server -D` to preview locally.
4. Deploy to GitHub Pages / Netlify / Cloudflare Pages.